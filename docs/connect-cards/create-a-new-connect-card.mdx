# Create a New Connect Card

import ReactPlayer from 'react-player';

<div className='player-wrapper'>
<ReactPlayer url="https://vimeo.com/manage/videos/678201930"
    width="100%"
    height="100%"
    controls={true}
    playing={true}
     />
</div>

To create a new connect card, start by going to Dashboard > Connect > Add New (or you can also hover over the + New menu in the admin toolbar at the top of the page). Select Connect Card to create a new connect card.
Give your connect card a Title and add some content to the card with the block editor.
Set the Featured Image and the Cover Image for the connect card.
Add a short description to appear under the title of the connect card.
Add an optional call to action using a button or form, or even set the connect card to redirect to a specific URL.